export const connectSSH = (connection) => {
    return (dispatch) => {
      // Logique pour établir la connexion SSH
      console.log('Connecting to SSH:', connection);
      // Dispatcher des actions pour mettre à jour l'état
    };
  };
  